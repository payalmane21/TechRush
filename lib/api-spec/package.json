{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs');const f='../../lib/api-zod/src/generated/api.ts';fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace(/from 'zod'/g,\\\"from 'zod/v4'\\\"))\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.23.0"
  }
}
